import 'package:flutter/material.dart';

const circularRadius50 = BorderRadius.only(
  bottomLeft: Radius.circular(150),
  bottomRight: Radius.circular(150),
);

const primaryColor =Color.fromRGBO(10, 26, 50, 1);

const circularRadiusTop50 = BorderRadius.only(
  topLeft: Radius.circular(50),
  topRight: Radius.circular(50),
);
const height10 =  SizedBox(height: 10.0) ;
const height20 =  SizedBox(height: 20.0) ;
